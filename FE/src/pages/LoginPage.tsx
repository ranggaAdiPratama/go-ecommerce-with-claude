import { usePageTitle } from '../hooks/usePageTitle';

export const LoginPage = () => {
    usePageTitle("Kalem");

    return (
        <div>
            <h1>Kalem, anjing</h1>
        </div>
    );
};